# StatGI 使用说明

原神挂机收益统计器 —— 挂机时自动统计捡到的摩拉、材料、圣遗物。

不用插件、不读游戏内存、不碰游戏，**只用屏幕识别**，开着软件正常打游戏就行。

---

## 一、快速开始（三步）

1. 打开原神，让游戏画面显示出来
2. 打开 StatGI（双击 `StatGI.exe`；第一次打开要等几秒，正常）
3. 在「启动」页点「**开始监测**」

然后正常打游戏，捡到东西会自动记账。再点一次就是停止。

第一次打开如果 Windows 弹出蓝色提示，点「更多信息」→「仍要运行」。

![启动页](使用说明配图/01_启动.png)

---

## 二、四个页面

**启动** —— 开始 / 停止监测。监测中会在下方显示这一轮的时间和收益。

**收益统计条** —— 桌面上那个小悬浮窗的设置（显示什么、多透明）。

**收益记录** —— 每一轮监测的明细，可以收藏、删除。

**设置** —— 识别、外观、行为、热键、更新……都在这儿，分 6 类。

侧栏下面还有个「**公告**」，点开看更新说明。

![公告页](使用说明配图/11_公告.png)

---

## 三、能统计什么

**摩拉** —— 提示里出现「摩拉 +xxxx」时累加

**材料** —— 按名字分开统计，比如「破损的面具 ×12」

**圣遗物（狗粮）** —— 出现一次算 1 个

原理是**读屏幕上的掉落提示文字**，所以提示文字越清楚，统计越准。

同一条提示一直显示只会算一次，不会重复记账。

---

## 四、关于准确度

识别靠读屏幕，偶尔会有出入，这是正常现象：

- 掉落提示一闪而过、没被看到 → 那一次会漏掉
- 画面糊、界面缩放过大 → 认字可能出错

想更准一点：

- 用**纯色背景**（设置 → 外观 → 自定义背景图片 → 清除），别用花哨的背景图
- 设置 → 识别 → 「检测间隔」「文字识别频率」都选「性能」
- 设置 → 识别 → 「画面变化灵敏度」选「性能」

---

## 五、桌面悬浮窗

在「收益统计条」页点「打开统计条」，桌面上会多一个小窗口，实时显示摩拉 / 材料 / 狗粮。

- 按住它可以直接拖到任意位置
- 「统计条透明度」调低一点，可以少挡游戏画面
- 三个格子显示什么、图标长什么样，都能在设置里改

这个窗口**直播也能用**：OBS 里加「窗口捕获」选中它就行（真透明，没有黑边）。

---

## 六、收益记录

每一轮「开始 → 停止」算一条记录，记着时间、时长、这一轮的收益。

每条记录右边有三个按钮：

- **查看明细** —— 展开看这轮掉了哪些材料
- **★ 收藏** —— 把这条记录**复制一份**存进收藏夹
- **🗑 删除** —— 删掉这条记录

![收益记录](使用说明配图/03_收益记录.png)

点顶部的「**收藏夹**」按钮可以切换过去看收藏。

收藏是**复制**，所以原记录删掉了、甚至清空了全部记录，**收藏夹里那份还在**。适合存「今天运气特别好」的记录。

![收藏夹](使用说明配图/04_收藏夹.png)

点「**编辑**」进入编辑模式：

- 每条记录左边出现勾选框
- 可以勾选多条，**批量收藏**或**批量删除**

![编辑模式](使用说明配图/05_编辑模式.png)

---

## 七、设置里常改的几项

想换界面颜色 → 设置 → 外观 → 背景颜色 / 强调色（还能自己调色）

卡片太透明或太实 → 设置 → 外观 → 卡片透明度

![外观设置](使用说明配图/06_设置_外观.png)

改全局热键 → 设置 → 行为 → 全局热键（开始/停止监测、显示/隐藏统计条）

每天几点重置数据 → 设置 → 统计 → 换日刷新数据

只统计某几样 → 设置 → 识别 → 识别哪几样

![识别设置](使用说明配图/07_设置_识别.png)

换更新渠道 → 设置 → 其它 → 更新渠道（国内推荐 Gitee）

---

## 八、常见问题

**一直识别不到东西？**
看看设置 → 行为 → 「只在原神前台时识别」是不是开着 —— 开着的话，切到别的窗口就会停止识别。
另外确认游戏没被别的窗口完全盖住。如果还是不行，把游戏改成「窗口」或「无边框窗口」模式再试。

**数字比实际少？**
掉落提示消失太快、或者被别的界面挡住，就会漏。可以把设置 → 识别 →「防重复窗口」调短一点。

**提示「程序已经在运行了」？**
右下角托盘里找它，点一下就能打开。软件只允许开一个，不然会重复统计。

**窗口找不到了？**
看右下角托盘图标，双击打开。

**怎么彻底退出？**
托盘图标上右键 → 退出程序。直接点右上角的 ✕ 只是最小化到托盘，监测还在继续。

**更新之后设置还在吗？**
在。更新只会替换程序，`config` 和 `data` 两个文件夹都会保留。

**会有更新提醒吗？**
会。打开软件会自动检查，发现新版本时左下角会闪红色提示，点一下就能更新。

![检测到新版本](使用说明配图/10_设置_其它_检测到新版本.png)

---

## 九、注意事项

- 软件**不读游戏内存、不做注入、不控制游戏**，只用屏幕截图 + 文字识别
- 识别结果存在本地 `data` 文件夹，**不会上传**
- 删除程序时，`config` 和 `data` 文件夹里的设置和数据也会一起删掉，介意的话可以先备份

---

## 十、反馈

QQ 群：**1125970274**

GitHub：https://github.com/Cash-553/stat-genshin-impact
