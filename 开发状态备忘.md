# StatGI 开发状态备忘

> 这个文件是给"人和 AI 都会忘"准备的。**每次接着干活前先看这里。**
> 最后更新：2026-09-23

---

## 一、当前版本状态

| 项 | 状态 |
|---|---|
| 当前版本号 | **v0.9.2（已打包，未发布）** |
| v0.9.2 的包 | ✅ 已打：`发布版\StatGI_v0.9.2\`（431 文件 / 275.8 MB） |
| v0.9.2 的 zip | ✅ 已压：`发布版\StatGI_v0.9.2.zip`（120.6 MB，**不含使用说明**） |
| v0.9.2 冒烟测试 | ✅ 通过（存活 25 秒、无 `data\error.log`；zip 解压再跑也通过） |
| v0.9.2 代码 | ✅ **已推送**（GitHub + Gitee，`af536bc..43f92c3`） |
| v0.9.2 公告 | ⏸ **已写好但故意没推**（本地提交 `2bc47f2`）—— 用户要先在软件里看效果 |
| v0.9.2 更新说明 | ⏸ 同上（`version.json` 的 `version`/`notes` 都写好了，也没推） |
| v0.9.2 的包 | ✅ 已打：`发布版\StatGI_v0.9.2\`（431 文件 / 275.8 MB） |
| v0.9.2 的 zip | ✅ 已压：`发布版\StatGI_v0.9.2.zip`（120.6 MB，**不含使用说明**） |
| v0.9.2 冒烟测试 | ✅ 通过（存活 25 秒、无 `data\error.log`；zip 解压再跑也通过） |
| ⚠ 使用说明 | **用户说还没写完，先不要打进包**（`使用说明.md` 也没更新到 v0.9.2） |
| v0.8 / v0.9 / v0.9.1 的包 | **全都在，一个字没动**（用户明确要求旧版本别删） |

### ⏸ 公告「待发布」的准确状态

- 公告正文已写好，成品在 `发布版\公告\notice.json`（v0.9.2 那条排最前）
  和 `发布版\公告\公告草稿_v0.9.2.txt`
- **本地已提交（`2bc47f2`），但没 push** → 用户那边还看不到
- 远程现在仍是 **v0.9.1 / 2 条公告**（已核实）

**要发布时只差一步：**

```
git push origin main          # 推 2bc47f2
```

⚠ 沙箱会挡 git 凭据助手（`couldn't create signal pipe, Win32 error 5`），
申请一次 `danger-full-access` 就能推。

⚠ **发布前最好先跑一次发布工具**：它会切 Gitee 分卷、算 sha256，并**自己把
`version.json` 的 `update` 那段写对**。现在 `update` 里还是 v0.9.1 的地址，
所以公告一推出去，老用户看到「0.9.2 有更新」但下载会失败。

### 公告预览（怎么给用户看）

```
python _morph\_preview_notice.py
```

它用**软件自己的公告页**（`qt_pages.PageNotice`）渲染待发布的那份，出图到
`_morph\_verify\B1_notice_preview.png`。给 `qt_notice.cache_file` 打桩，
读 `_morph\_pending_notice\notice.json`，不动真实配置和缓存。


> **版本号写在 `statgi_qt/qt_pages.py` 的 `VERSION`**，别处都从它取
> （侧栏左下角以前写死 V0.9，改版本号会漏，现在改成引用 VERSION 了）。

### 打包流程（v0.9.2 走通的版本）

```
cd E:\收益识别
python -m PyInstaller StatGI.spec --noconfirm
python _morph\_make_package.py      # dist\StatGI -> 发布版\StatGI_v0.9.2\（已存在就停手）
# 改完东西要重打时：
python _morph\_refresh_package.py   # 刷新 _internal + 重置 config + 清空 data
python _morph\_smoke_test.py        # 冒烟：存活 25 秒 + 无 error.log
python _morph\_make_zip.py          # 压 zip（内容直接放根目录）
python _morph\_verify_zip.py        # 解压再跑一遍，验真正发出去的东西
```

- `_morph\_make_zip.py` 会**先清空包的 data/** 再压，保证 zip 干净
- ⚠ 这几个脚本都写死了"目标目录已存在就停手"，不会覆盖旧版本

### 打包时踩的坑（v0.9.2 新踩的）

1. ❌ **`app.py` 里 `_log()` 用 `__file__` 算目录** —— 冻结后 `__file__`
   指向 `_internal\`，启动日志被写进 `_internal\data\`。
   要用 `paths.app_dir()`（exe 旁边）。
2. ❌ **别把"源码版对照测试"和"exe 测试"放在同一条命令里连着跑** ——
   两个都建同一个单实例互斥体，先起的那个占住，后起的弹
   "程序已经在运行"就**干净退出（rc=0）**，看起来像"打包版起不来"。
   我就是这么误判了一轮。要测 exe 就单独测，别同时跑源码版。
3. 打包版是 `console=False`，**看不到任何输出**。排查启动问题时设
   `STATGI_STARTUP_LOG=1` 再启动，会写 `data\startup.log`
   （默认不写，免得给用户多个文件）。

### 使用说明（用户手册）

- 源文件是仓库根目录的 **`使用说明.md`**（给 GitHub / Gitee 网页看）
- 配图在 **`使用说明配图/`**（11 张，由 `_morph/make_shots.py` 生成）
- **PDF 版**：`使用说明.pdf`（9 页，A4，带配图），给用户单独发
- 但 **PyInstaller 的 `datas` 会进 `_internal\`，不会出现在 exe 旁边** ——
  所以说明书必须**手动复制**进发布文件夹
- 步骤：

```
python _morph\md2txt.py      # 生成 txt（放进安装包）
python _morph\md2pdf.py      # 生成 PDF（单独发给用户）
```

- ⚠ **每次重新打包后都要跑一遍 md2txt**，否则新包里没有说明书
- `md2txt.py` 的目标路径现在会**从 qt_pages.VERSION 读**，不再写死版本号
- 🔴 **v0.9.2 暂时不打包说明书**（用户说还没写完）。
  而且 `使用说明.md` 目前**还没更新到 v0.9.2** —— 缺：黑名单/白名单、
  收益记录的两级展开、记录的名称/备注、预设复制、编辑器这些。
  用户写完后再跑 `md2txt.py` + 重压 zip。

### 截图是怎么来的（重要技巧）

`_morph/make_shots.py` 用离屏模式跑起整个窗口再逐个页面截图。

**关键**：离屏模式默认找不到字体，**中文会全渲染成方块**。
必须在 `import PySide6` **之前**设好：

```
os.environ["QT_QPA_PLATFORM"] = "offscreen"
os.environ["QT_QPA_FONTDIR"] = r"C:\Windows\Fonts"     # ← 这一行是关键
```

⚠ 另外：离屏截图只 `processEvents()` 不够，卡片会叠在一起看着像布局 bug ——
要显式把各级 `layout().activate()` 跑一遍，几何才是对的（v0.9.2 踩过）。


### PDF 生成的坑

`_morph/md2pdf.py` 优先用 **Edge 无头模式**（`--print-to-pdf`），
失败才退回 Qt 的 QPrinter。踩过的四个坑：

| 坑 | 现象 | 解法 |
|---|---|---|
| Edge 的配置目录放在系统临时目录 | `Lock file can not be created! 0x5` | 配置目录挪到工作区内 `_morph/_edge_profile` |
| 图片用相对路径 | **不报错**，PDF 里只画个破图占位符 | 图片 base64 内嵌进 HTML |
| 没删旧 PDF | Edge 跑失败也**误报成功**（判断"文件存在"拿到了旧文件） | 生成前先 `os.remove(PDF_OUT)` |
| Qt 把图放大到整页宽 | 11 张图变成 13 页 | `<img>` 上加 `width="660"` |

- **沙箱会拦 Edge**（命名管道创建失败 → `platform_channel 拒绝访问`）。
  遇到就申请一次 `danger-full-access`，能直接出 9 页的正式版；
  否则会退回 Qt，排版朴素且页数翻倍

### 文档现状

| 文件 | 给谁看 | 说明 |
|---|---|---|
| `使用说明.md` + `使用说明.pdf` | **用户** | 用户手册（md 给网页，pdf 单独发） |
| `使用说明配图/` | —— | 手册的 11 张截图 |
| `README.md` | 开发者 / 访客 | 功能特性、技术方案、目录结构 |
| `statgi_qt/说明.txt` | 开发者 | 打包步骤、踩坑记录 |
| `开发状态备忘.md` | 自己 / AI | 待办、规则、坑（就是本文件） |
| ~~`使用说明.txt`（根目录）~~ | —— | **Tk 时代的老手册，已过期**，别再用了 |

### 材料库（自动登记的那些名字）

- 文件：`data/materials.json`，结构就是 `{"materials": [{"name":…, "icon":…}]}`
- 内置名单在 `materials_db.INITIAL_MATERIALS`（**84 个**，别写死 60）
- 「自动登记新材料」开着时，OCR 认到什么名字就往里加 —— 所以攒了一堆错词
- `materials_db.is_builtin(name)` 判断某个名字是不是内置的（不是就是自动登记的）

**一键重置所有人**：把 `materials_db.LIB_VERSION` 加一。

- `main.py` 启动时调 `materials_db.migrate_library(settings)`，
  比设置的 `materials_lib_version` 低就重置一次，然后写回版本号
- ⚠ **必须放在建窗口之前** —— Detector 一建实例就 `load_materials()`
  把名字读进内存，先清完再读才行
- 当前 `LIB_VERSION = 1`：0 → 1 那次会清掉自动登记的，只留内置 84 个

给用户的手动入口：`设置 → 开发 → 材料库 → 材料名单 [编辑…]`
（`qt_dialogs.MaterialDialog`：搜索 / 只看自动登记 / 勾选删除 / 双击改名 / 添加）

### 设置标签页

```
外观  识别  行为  统计  直播  升级  开发
```

- 「升级」= 原来的「其它」（只有更新渠道 + 版本更新）
- 「开发」= 开发者模式 + 开发者选项 + 材料库（默认隐藏，靠开发者模式开关显示）
- 改标签名要一起改的地方：`TABS`、`_build_*` 的 `tb` 变量、
  `_tab_dots`/`_scroll` 的键、`_make_dev_items` 里的 `self._inner["开发"]`

---

## 二、Git 状态

- **已全部推送**（`main...origin/main` 同步，`git log origin/main..HEAD` 为空）
- 工作区干净
- v0.9 的改动已提交，最新提交看 `git log --oneline -1`

- 推送：`git push`（origin 配了双地址，GitHub + Gitee 一起推）
- 沙箱会挡住 git 的凭据助手（`couldn't create signal pipe, Win32 error 5`）。
  **申请一次 `danger-full-access` 就能推**，不用让用户自己开终端

---

## 三、待办清单

### 发 v0.9.2 时要做

**已经做完的：**
- ✅ 版本号改成 0.9.2
- ✅ 打包 → `发布版\StatGI_v0.9.2\`（431 文件 / 275.8 MB）
- ✅ 压成 `发布版\StatGI_v0.9.2.zip`（120.6 MB，根目录直接放文件）
- ✅ 冒烟测试通过（存活 25 秒、无 error.log）+ zip 解压再跑也通过
- ✅ 公告**草稿** `发布版\公告\公告草稿_v0.9.2.txt`
- ✅ 旧版本（v0.8 / v0.9 / v0.9.1 的包和 zip）**原封不动**

**还没做的（等用户）：**
1. **公告推送** —— 用户要先在软件里看一眼效果。
   现在只差 `git push origin main`（本地提交 `2bc47f2`）。
   - ⚠ **最好先跑发布工具**再推：它会把 `version.json` 的 `update`
     那段（分卷地址 + size + sha256）写对。现在那段还是 v0.9.1 的，
     公告一推出去，老用户会看到「有更新」但下载失败。
   - 或者用**公告编辑器**点「发布」（它自己 add/commit/push）
2. **发布工具**切 Gitee 分卷 → `发布版\发布\v0.9.2\`
   - 工具**按修改时间倒序列出所有 `StatGI_v*.zip`，默认选最新的** ——
     所以 v0.8 / v0.9 / v0.9.1 的旧 zip **留着不会影响它选对 v0.9.2**
3. **使用说明** —— 用户说还没写完，**先不要打进包**；
   而且 `使用说明.md` 本身也还没更新到 v0.9.2
4. GitHub Release 传 `StatGI_v0.9.2.zip`；Gitee Release 传分卷 + 一键安装.bat

**顺序**：打包 zip ✅ → 发布工具（切分卷 + 写 version.json 的 update）
→ 推公告 → 传 Release 附件。

**注意**：用户平时是从 `发布版\StatGI_v0.9\` 里跑 exe 的，那个文件夹留着别删
（里面有他的真实收益记录）；新包是 `发布版\StatGI_v0.9.2\`，多份并存。


### 识别准确度（可选，用户明确说先不动）

6. `detector.py` 里试过三次改动**全部撤销**了（详见「五之二」末尾的自查表），
   现在识别逻辑跟原版一致
7. 未解决的固有盲区：规则 11（6 个同名同数量的快速拾取）
8. 死代码：`_observe_event` 和 `_scan_region_rows` 定义了但从来没被调用

### 米游社札记（用户说"以后搞"）

9. 进度：**接口已经打通到一半**
    - ✅ 账号接口能用（`getUserGameRolesByCookie`），已查到用户的号：天空岛 / UID 18*****31
    - ❌ 札记接口 `hk4e-api.mihoyo.com/event/ys_ledger/monthInfo` 一直回 `未登录`
    - 试过 12 种请求组合（含完全照抄 BetterGI 的写法）全部失败
10. **卡点：缺 `e_hk4e_token`** —— UIGF 接口文档说这是《原神》网页活动的账号标识
    - 用户的 cookie 里没有它；建议去 Edge 的 `开发者工具 → 应用程序 → Cookie`
      里翻**其它域名**（不是 `www.miyoushe.com`）
11. 注意：BetterGI 那份实现**很可能也是坏的**（它的 cookie 只有 `_MHYUUID`，
    而且 `D:\BetterGI\log\logparse\` 下**从来没有生成过缓存目录** = 一次都没成功过）
12. 工具都还在：`ledger_test.py`、`cookie.txt`（已加 .gitignore，绝不提交）

### 已知问题（没解决）

13. **拖影（ghosting）** —— 用户说换到 Qt 之后已经好了，可以划掉
16. `_mys.py` 早就删了；米游社的探测脚本现在叫 `ledger_test.py`

---

## 四、之前讨论过、但没做的功能（备忘，别丢）

开机自启 / 停止时小结 / 材料搜索 / 掉率统计 / 收益趋势图 /
每日备份 / 设置导出导入 / 托盘菜单完善 / 界面缩放 / 多显示器 /
统计条吸附 / OCR 放大倍数档位 / 设置里显示实测耗时

---

## 五、图标系统（今天新加的）

- 界面图标 = **Lucide 矢量图标**（56 个），数据在 `statgi_qt/qt_icon_data.py`（123 KB）
- 控件在 `statgi_qt/qt_icon.py`：图标 + 弹簧 + 悬停助手
- 悬停效果：鼠标放到卡片/设置行上，图标**弹性放大 1.18 倍**（snappy 弹簧，不旋转）
- **应用图标（那个叶子 🍃）用 emoji**，不是矢量图标 —— 用户特意要求的
- 折叠菜单展开后，每一项是一张**子卡片**（比主卡片左右窄 12px，彼此无空隙）
- 全量图标画廊：`python _morph\gallery.py`（2112 个，可搜索）
- 加新图标：把名字加进 `_morph/fetch_icons.py` 的 `NAMES` 和
  `_morph/build_icon_data.mjs` 的 `USED`，跑

```
python _morph\fetch_icons.py
node --import ./_morph/register.mjs _morph/build_icon_data.mjs
copy _morph\qt_icon_data.py statgi_qt\qt_icon_data.py
```

---

## 五之二、「获得」栏的识别规则（重要！别再忘）

> 来源：`新建文件夹\StatGI_最终版_5行FIFO拾取Track_素材库性能优化_DeepSeek_Harness.md`
> （2604 行，第 3~17 章是规则，第 43~50 章是 10 个必测场景）
> ⚠️ `新建文件夹\` 在 .gitignore 里，**这几份文档不在仓库里，别删**

### 模型

把左下角「获得」列表建模成一个**有序 FIFO 队列**，最多同时显示 **5 行**：

```
[A, B, C, D, E]     满了
新事件 C 退出、F 从队尾进入
[B, C, D, E, F]
```

### 铁律

| # | 规则 | 说明 |
|---|---|---|
| 1 | **Track ≠ 物品名称** | `霜仙花` 连续捡 3 次是 **3 个独立 Track**，不是同一个 |
| 2 | **严禁用 Y 坐标做身份** | 下方提示会平滑补位到上方，`Y 变了 ≠ Track 变了` |
| 3 | **身份 = 有序队列连续性 + identity + 生命周期 + 前缀消失 + 漏读恢复 + 队尾新增** | Y 只作诊断 |
| 4 | **LCS 只用来找"保留关系"** | **不能**直接决定"这行是不是新事件"；比较对象要用标准化 identity，不是原始 OCR 文本 |
| 5 | **OCR 抖动要维持同一 Track** | `霜仙花×1 / 霜仙花× / 稻仙花×1 / 仙花岗1` 全都得归到同一个 Track |
| 6 | **数量丢失不能断链** | `霜仙花×1 → 霜仙花× → 霜仙花×1` 是同一个 Track。**数量不是身份** |
| 7 | **MISSING ≠ ENDED** | 状态机 `VISIBLE / MISSING / ENDED`；单帧没 OCR 到只是 MISSING，超时才 ENDED |
| 8 | **每个 Track 只入账一次** | `counted=False → _apply_event() → counted=True`，之后看 100 帧也不再记 |
| 9 | **不等两帧，立即统计** | 新 Track 可靠识别后**马上**入账。等两帧会让单帧成功的真实拾取**漏记** |
| 10 | **同名快速拾取必须 +5** | `霜仙花×1` 连来 5 次 = `+5`，不能去重成 +1，也不能 +2 |
| 11 | **超过 5 个靠队列** | 游戏最多显示 5 行 → 不能假设"可见行数 = 事件数"；靠"前缀出队 + 队尾进入 + 生命周期"逐个建立 Track。6 个同名同数量也必须分别统计 |
| 12 | **不能凭空推断屏幕外事件** | 只看到 5 个就维护 5 个；第 6 个**必须**有后续 OCR 证据才能记 |
| 13 | **同名序列按队列顺序** | `[霜仙花×3]` 少一个时，退出的必须是**第一个**，不能让生命周期错位 |
| 14 | **多个旧 Track 同时退出** | `[A,B,C,D,E] → [F,G,H,I,J]` 要解释成 5 个 END + 5 个 NEW，**不能**按名字 A→F 对应（哪怕名字全一样） |

### 三条"绝对禁止"

| # | 禁止 | 原因 |
|---|---|---|
| 15 | **禁止改统计层去减重复**（`stats -= duplicate`） | 统计层只接受已确认事件。正确做法是 Track 本身对 |
| 16 | **禁止用全局同名时间锁防重复** | `if identity in recent_events: return` 会把 `霜仙花×1` 连来 3 次错记成 +1 |
| 17 | **禁止靠降低 OCR 频率掩盖问题** | 提示会快速变化、5 行会批量退出、新提示快速补位。要先靠"变化检测 + Track 缓存 + 名称缓存"省工作量 |

### 10 个必测场景（第 43~50 章）

1. 同一行连续 OCR → 1 次
2. 数量丢失 → 不断链
3. OCR 名称抖动 → 同一 Track
4. 同名快速连续拾取 → +5
5. 同名 + 同数量 + 超过 5 个 → 分别统计
6. 补位（上面退出、下面顶上）→ 同一 Track
7. 批量消失（5 个一起换）→ 5 END + 5 NEW
8. OCR 单帧漏读 → MISSING，不是 ENDED
9. 真正消失后重新出现 → 新 Track
10. 同名 Track 不串号（生命周期不能错位继承）

### 本次改动 vs 规则（自查）

**结论：识别那处改动已经全部撤销，`detector.py` 回到原版。**

试过、但都撤了的三次改动，记下来免得以后重蹈覆辙：

| 试过的改法 | 为什么撤 |
|---|---|
| 数量增量补记（×1 → ×3 补记 +2） | 前提就错了 —— 用户确认**每次捡都会出新的一行**，不会在同一行累加。留着的话 OCR 把 ×1 看成 ×7 就会多记 6 个 |
| 按名字相似度认领旧 Track（防 OCR 错字多记） | 量过真实材料名：「士官的徽记 / 尉官的徽记」「浊水的一掬 / 浊水的一滴」相似度 0.80，而 OCR 错一字是 0.67~0.86 —— **两个区间完全重叠**，按相似度认领会把不同材料合并（漏记） |
| 对不上的行只要不在最后一行就当噪声 | **会造成漏记**：两次 OCR 之间连捡 2 个时是 `[…, 新1, 新2]`，新1 不在最后一行 → 被吞掉。用户实测"准确度比以前差"就是这么来的 |

**未解决**：规则 11（6 个同名同数量的快速拾取）是这套模型的**固有盲区** ——
列表内容完全相同时，出队+入队会被 LCS 全配上 → 新的那次数不出来。
这是设计限制，不是笔误级的 bug（详见「规则 11 讨论」）。

---

## 五之三、文档要求但没做的 —— 评估结论（2026-09-21 实测）

> 结论：**基本都不值得做。** 别再纠结这些了。

### 实测数据

```
名单规模        材料 574 + 圣遗物 299 = 873 个
单次 _sim       2.8 微秒
一次 _fuzzy_match  1.2 ~ 2.9 毫秒（按长度分组后扫 150~400 个候选）
占 OCR 周期     1.19 ms / 150ms = 0.79%
```

**参照物：OCR 本身一次约 660 毫秒。** 纠错那 1~3 毫秒是它的 0.2%。

### 逐项评估

| 文档要求 | 结论 | 理由 |
|---|---|---|
| 纠错缓存 `correction_cache` / LRU | ❌ **不做** | 省 0.79% 的周期，而 OCR 占 660ms。文档里说的"卡顿"是**长度分组索引之前**的事，那个优化已经在代码里了 |
| 一维 LCS 优化 | ❌ 不做 | `_sim` 一次 2.8 微秒，优化它省不到 1 毫秒 |
| 统一名称索引 | ❌ 不做 | 精确查表本来就是 O(1)，只有纠错路径才遍历；省不到 1 毫秒 |
| 字符快速过滤 | ❌ 不做 | 有了长度分组，收益已经很小 |
| 名称解析 / Track 解耦 | ❌ 不做 | **纯重构，用户看不到任何变化**，风险却很高。除非要大改识别逻辑再动 |
| 性能 Profiling | ❌ 不做 | 现在没有卡顿可查。等真出现卡顿再测 |
| Pending Track 机制 | ❌ 不用做 | 已被最终版的 **MISSING 状态**取代，而 MISSING **已经实现了** |
| **主界面检测用"固定 HUD 特征"** | ⚠️ **看情况** | 这**不是性能问题，是正确性问题** —— 关系到会不会在**背包 / 角色 / NPC 对话 / 加载界面**里误统计。现在代码是"单帧相似度 ≥0.70 + 2.5 秒离开缓冲"。**只有用户实际见过这类误判才值得改** |

### 建议把精力放到别处

1. 打包 v0.9（现在包是旧的）
2. 拖影问题（一直没根治）
3. 规则 11 的固有盲区（6 个同名同数量的快速拾取）
4. 米游社札记（卡在 `e_hk4e_token`）

---

## 六、容易踩的坑（都踩过了，别再踩）

| 坑 | 说明 |
|---|---|
| **PyInstaller 的 datas 会进 `_internal\`** | 不是 exe 旁边！读资源要走 `paths.resource_file()` |
| **`from qt_theme import TEXT` 会把颜色绑死** | 必须 `import qt_theme as T` + 用 `T.TEXT`，否则换主题不生效 |
| **`QIcon` 不支持 `setDevicePixelRatio`** | 会把大图当大尺寸塞进小图标位 → 只显示左上角一块。要注册 1×/2×/3× 多张图让 Qt 自己挑 |
| **Qt 里鼠标移到子控件上，父控件会收到 Leave** | 悬停检测要用 `HoverHelper`（按光标是否还在自己矩形内兜底） |
| **bat 要按 OEM 代码页写** | 写 UTF-8 中文会乱；写 GBK 又加 `chcp 65001` 一样会乱。用 `GetOEMCP()` |
| **`sys.stdout = io.TextIOWrapper(...)` 会关掉原 buffer** | 用 `sys.stdout.reconfigure(encoding='utf-8')` |
| **PowerShell 里内联 `python -c` 带引号会炸** | 写成临时 .py 文件跑 |
| **`import.meta.url` 的 pathname 是 URL 编码的** | Node 里要用 `fileURLToPath()` |
| **`error.log` 在 `data\` 不在 `config\`** | 查"有没有报错"要看 `data\error.log`。查错地方会一直以为没问题 |
| **`QColor.setAlpha()` 只接受 0~255** | 超了会报警并**疯狂刷 error.log**（一次就刷出 2000 多行）。算透明度一定要 clamp |
| **`rebuild_ui()` 会把旧页面 `setParent(None)`** | 之后再拿 `self.window()` 就**找不回主窗口**了 → 弹窗跑到屏幕角落。要在 rebuild **之前**先把主窗口存下来（`win = self.win`） |
| **`QMessageBox` 的位置要在 `show()` 之后摆** | Qt 显示对话框时会自己重新定位一次，`exec()` 之前 `move()` 的会被覆盖掉 |
| **长度不限的文字放卡片右边会撑宽整行** | 长文件名/长路径必须省略 + 限宽，完整内容放 tooltip |
| **⚠ 别删用户的东西** | 打包/发版时看到"过期的包、旧文件"**先问再动**。发布工具只是**列出** `StatGI_v*.zip` 让用户选，不会挑错版本 —— 我拿"怕发布工具认错"当过理由删掉 `StatGI_v0.9.zip`，结果那是用户的东西。删任何不在 git 里的产出前必须先问。**2026-09-23 用户又专门交代了一次「旧版本的东西别给我删掉」** —— 所以 v0.9.2 打包时 v0.8 / v0.9 / v0.9.1 的目录和 zip 一个都没动，`发布版\` 里现在并存 4 个 zip。 |

---

## 七、环境

- Python：`C:\Users\ASUS\AppData\Local\Python\pythoncore-3.14-64\python.exe`
- Node：`D:\node\node.exe`（v24，能直接跑 .ts）
- 屏幕：1920×1080，DPI 106（110%）
- 测试时**一定要把 `config_manager.CONFIG_DIR` / `SETTINGS_FILE` 指到临时目录**，
  别碰用户真实的 `config/settings.json`
