# 监测悬浮窗编辑器 · 交接说明

> 给下一个接手的人（或新会话的我）看。代码在 git 里，这份在仓库里，**记忆不会丢**。

## 现在什么状况

`statgi_qt/qt_bar_editor.py` 这轮**重写过了**，现在是用主界面现成控件搭的：

```
左侧栏      NavButton（qt_window）—— 图标 + 名字、悬停弹动、选中高亮
页面        QStackedWidget，两页：预设 / 项目
预设页      一套预设一张 Accordion 折叠卡片 + 「新建预设」
项目页      顶上标签栏（照抄 PageSettings「外观/识别/行为」的做法）
            每个标签前面一个 Switch；下面是当前项目的设置
每行设置    SettingRow（qt_widgets）
每组设置    Accordion（qt_widgets）
```

**一个新控件都没造。** 这是关键 —— 之前手搓 QTreeWidget 当侧边栏，
搞出二十多个 bug、来回改了二十多轮。

## 已完成（最近三轮）

- 编辑器重写（用主界面控件）
- 数据源「固定文字」→「**监测状态**」（正在监测 / 已停止）
- 标签前勾选框 → **Switch**
- 标签上**右键 → 删除项目**
- 「**把这一项的外观应用到全部项目**」
- 40 多条简介改成专业说法

## 已完成（8 条意见的收官三条）

- ① 侧栏两个选项文字周围那圈**黑底** —— 已修
- ② 项目标签文字外面那**一个框** —— 已修
- ⑤ 选材料**能搜索** —— 已修（`MaterialPicker`）
- 🐞 顺手修掉：**右键菜单一按就 `NameError`** —— `QMenu` 从头到尾没 import

## 已完成（图标 + 预设这一轮）

### 🐞 图标「上方」实际画在左上方

`qt_bar._build_inner()` 里 **`icon_label` 从来没设过对齐**。
「上方」时 label 横向撑满卡片（96 宽的卡片里 label 占 80），
而 QPixmap 默认左对齐 → 图标贴左边。

实测（96x96 卡片、icon_size=46）：

| icon_pos | 修前图标中心 x | 修后 |
|---|---|---|
| `top` | **30**（卡片中心 48） | **48** ✅ |
| `left` | 30 | 30（本来就该在左边） |

修法：`top` → `AlignHCenter|AlignVCenter`；`left/right` → `AlignCenter`。
判据是**数图标像素的水平中心**，别靠眼睛看。

### 图标库只留三个 + 支持自己导入

- 新增 `icons_lib.py`：`DEFAULTS` = 摩拉 `_bar_slot1.png` /
  材料 `_bar_slot2.png` / 狗粮 `_bar_slot3.png`
- 80 多个内置材料图标**移到 `icons/_已移除/` 备份**（不是删 ——
  `icons/` 在 .gitignore 里，删了没有 git 可回滚）
- 编辑器「图标」页：下拉只剩三个 + 自己导入的，另加
  **导入…** / **删掉** 两个按钮
- `mora.png` / `artifact.png` 留着兼容老配置，但**不列进下拉框**，
  也不许删

### 预设：改动落在当前配置，并明确提示

- 经典 / 直播间是**内置模板，定义永远不动**
- 标题下面新增一行提示，三种状态：
  - `当前就是「经典」预设的原样`
  - `⚠ 已偏离「经典」（内置模板）——改动只影响当前配置，「经典」本身没动`
  - `⚠ 当前配置是自定义的（基础是「直播间」这套内置模板，已经改过）`
- `_detect_preset()` 认「和预设完全一样」，`_closest_preset()` 认
  「项目 id + 窗口排布对得上」用来找基础。
  ⚠ 比较前两边都要过一遍 `bar_items._fill_item()` ——
  `load()` 会给每项补默认字段，而 `all_presets()` 是原始项，
  字段数不同直接比 JSON 永远不相等。

### 🔧 自动更新不再冲掉用户导入的图标

`qt_updater` 的 robocopy 原本只跳过 `config` 和 `data`，
`icons\` 会被新版本整个替换 → 用户自己导入的图全没。
现在 `robocopy` 加 `/XD icons` 整个跳过，改由
`icons_lib.sync_bundled_icons()` 在**启动时**补齐新版本自带的图标：

- 三个 slot + `mora`/`artifact` → 永远按新版本覆盖（外观基线）
- 其余 → **只在缺失时补**，不覆盖同名文件（用户改过的得以保留）

⚠ `python` 在打包版里不存在（PyInstaller），所以**不能在 bat 里调 Python**，
只能在主程序启动时补。

## 已完成（界面这一轮）

- **图标位置加「下方」** —— 下拉现在是 上方/下方/左侧/右侧。
  `qt_bar._build_inner()` 多一个 `bottom` 分支：文字在上、图标在下，
  同样要 `AlignHCenter|AlignVCenter`（不设就又是贴左边那个坑）。
  实测 96x96 卡片：上方图标 y 起点 17、下方 58，两者都水平居中在 x=48。
- **删掉右下角「完成」按钮** 和 **「共 N 个项目，显示 M 个」那行字**。
  `_set_status()` 保留成空实现（还有好几处在调它）。
- **项目页上方加一行「正在编辑哪一项预设」**（`edit_hint`）：
  - `正在编辑「经典」预设`
  - `正在编辑「经典」的改动（经典 这套预设本身没动）`
  - `正在编辑自定义配置（从「直播间」改来的）`
- **项目页右下角加「保存 / 取消」**，两个都回预设界面：
  - `_save_and_back()` → `_save()` 后 `_refresh_presets()`
  - `_cancel_edit()` → 从磁盘 `load()` 回滚未保存改动后再回预设界面
  （`_save()` 是即时落盘的，所以磁盘上的就是最后一次保存的状态）
- 标题下面那行 `preset_hint` **删了** —— 和项目页的 `edit_hint` 重复。

### ⚠️ 一个没能复现的用户反馈

用户说「更改内置预设后会立刻生成一套新预设，在预设里面能找得到」。
我把所有路径都试了（套用经典 → 改 value_size → 改 show_title →
`_on_change` → `_apply_to_all` → 切页 → 重开），`bar_presets.json`
始终是空的；`git log -S 'save_preset'` 查历史也**从来没有**自动生成的代码。
唯一能生成用户预设的是预设页那个「把现在的样子存为预设…」按钮。

**2026-09-23 后来澄清了**：他真正想要的是「预设只读，想改必须复製之后改」。
所以按那个方案做了（见下），这个反馈不再单独追。

## 已完成（预设只读 + 复制这一轮）

### 预设只读：经典 / 直播间进去只能看，不能改

用户原话：「进去后你能看到所有配置，但是所有的项目全是灰的无法修改，
上方加一条提示此预设为内置预设，无法修改，如需修改，请复制后再进行修改」

- `_is_readonly()` = 当前 `_preset` 是内置的（经典 / 直播间）
- 项目页上方多一条橙色提示条 `ro_banner`，
  文字：`内置预设无法修改，请复制后再修改（回预设页点「经典」那行的「复制」）`
- 只读时：设置区控件禁用、保存禁用、取消变「返回」、「＋」禁用、
  标签上的 Switch 禁用；`_save()` 和 `_on_change()` 也加了 Guard，
  就算被程序调用也改不动、不落盘

⚠ **别用 `scroll.setEnabled(False)` 做只读** —— 那样折叠分组也点不开了，
用户就**看不到**里面的配置。正确做法是
`_set_controls_enabled()`：只禁 `QAbstractSlider / QComboBox / QLineEdit /
QAbstractButton`，分组标题保持可点。实测 6 个分组全可点、里面 20 个控件全禁用。

⚠ `_load_item()` 最后必须再调一次 `_apply_readonly()`：它会重建图标下拉等
控件，新控件的 enabled 是默认值（可点），不重套一遍只读就失效了。

### 预设卡片的五个按钮：使用 / 查看 / 复制 / 改名 / 删除

预设页顶部还有一条提示：**建议先创建预设，再进行修改**。

**「使用」和「查看」是分开的**（用户要求）：

| 按钮 | 干什么 | 结束在哪 |
|---|---|---|
| **使用** | 就用这套配置，不进去看 | `accept()` 关窗，**回主界面** |
| **查看** | 进去看它长什么样 | 进项目页（内置的是只读） |
| **复制** | 复制一份可以改的（内置也能复制） | 复制完直接进项目页编辑 |
| **改名** | 给这套预设改名 | 留在预设页 |
| **删除** | 删掉这套预设（内置的删不掉） | 留在预设页 |

⚠ `_use_preset()` 里必须 `self._preset = ""`：它是「使用」不是「查看」，
不清掉查看态的话 `_save()` 会被只读 Guard 挡住，配置就存不下去。

（旧方法名 `_enter_preset` 已改名 `_view_preset`；`_apply_preset` 保留为
`_view_preset` 的别名，给验证脚本用。）

## 已完成（收益记录 + 黑白名单这一轮）

### 收益记录：**两级展开**的折叠卡片 + 名称 / 备注

一条记录是**两级展开**（用户反复纠正过，别再搞错）：

```
收起      🗓 日期　开始 → 结束　时长      [✎][★][查看明细][▸][🗑]
                                              ↑ 收起时就必须看得到
展开一级  摩拉 xx　狗粮 xx　材料 xx       ← 总数（不是明细）
展开二级  破损的面具 ×12  牢固的箭簇 ×6 …  ← 材料明细全列出
```

⚠⚠ **「查看明细」按钮必须挂在卡片头部**（卡片的直接子级），
**不能在 `head_body` / `detail` 里面** —— 那两个容器收起时是隐藏的，
按钮塞进去用户就会报"折叠卡片里没有查看明细这个选项"（真报过两次）。

- 两个状态分开存在 `PageRecords` 上：
  - `self._open` = 第一级展开了（看得到总数）
  - `self._mats` = 第二级展开了（列出了材料明细）
- 卡片上三个容器，靠 `setVisible()` 控制：
  `head_body`（总数）/ `detail`（材料列表）/ `mats_box`（材料行）
  ⚠ 判断显没显示要用 `isHidden()`，别用 `isVisible()`（见下面第 2 条坑）
- 头部「查看明细」按钮**两级一起管**（点它 → 总数 + 明细都出来）；
  点名称 / 备注 / 箭头只切第一级，但按钮文字要跟着对上
  （`_sync_btn()` 统一刷）
- 日期 / 时间 / 时长**只在卡片标题（默认名）里出现一次**，展开区不重复放
- **名称** = 卡片标题，默认「日期 + 开始 → 结束 + 时长」
- **备注** = 标题下面那行小字（没备注时显示「（没有备注）」）
- 编辑入口 = 主卡片右边**一个纯图标按钮**（铅笔），点了弹
  `RecordEditDialog`（名称 + 多行备注；占位文字就是「随便写点什么」）
- 数据层（`sessions.py`）：`make_record` 多 `name` / `notes` 两个字段
  （空 = 用默认名），配 `display_name` / `display_notes` / `default_name` /
  `update_record`
  ⚠ `update_record` **原记录和收藏夹副本都要改** —— 收藏夹存的是深拷贝，
    只改一边两边名字会不一致

### 预设「使用」按钮：**不关闭编辑窗口**

用户报过两次「使用预设后编辑窗口还是自动关」。真凶是
`_use_preset()` 末尾那句 **`self.accept()`** —— 点「使用」直接把整个
对话框关掉了。

现在：应用预设 → 切到**项目页**继续编辑，**窗口不关**。

⚠ 别再把 `accept()` 加回 `_use_preset`。`_save_and_back()`（保存按钮）
也早就改成原地保存、不跳回预设页了。

| 按钮 | 现在干什么 |
|---|---|
| 预设卡的**使用** | 应用这套配置 → 进项目页继续编辑，**不关窗口** |
| 预设卡的**查看** | 进项目页看（内置的只读） |
| 项目页的**保存** | 存盘，**留在编辑窗口** |
| 项目页的**取消** | 撤销未保存改动，回预设页 |
| （只读时）**返回** | 回预设页 |


### 版本号

**`statgi_qt/qt_pages.py` 的 `VERSION` = `0.9.2`**（唯一源头，别处都从它取）。
`发布版/公告/version.json` 和 `notice.json` 还是 **v0.9.1** ——
那是「已发布的版本」，发新包时由发布工具自动改，别手改。


### 设置 → 统计：黑名单 / 白名单

层级（用户指定，**主卡片没有开关**）：

```
黑名单管理            ▸ 展开（Accordion）
  ├ 材料黑名单        [开关]  ← SwitchAccordion，开关在卡片头上
  │   └ 配置名单      ▸        ← 开关打开后才出现
  └ 圣遗物黑名单      [开关]
      └ 配置名单      ▸
白名单管理 …（同上，材料 / 圣遗物）
```

- 生效规则（`filters_db.allows`，**白名单优先**）：
  - 白名单开着且非空 → 只记名单里的
  - 黑名单开着 → 名单里的一律不记
  - 白名单开着但为空 → 什么都不记
- 接在 `detector.py` 的材料 / 圣遗物每个判定返回点上（`_filter_allows`）
- 配置名单弹窗 `qt_dialogs.NameListDialog`：左边可选（可搜索）右面已选，
  双击搬一条、Ctrl/Shift 多选后点「→ 加入 / ← 移出」批量搬

#### 空名单提醒（用户要求）

离开设置页（`PageSettings.on_hide`）时检查「**开着但空着**」的名单，
弹窗**居中显示在应用窗口正中**：

- **白名单空**：「材料白名单」为空，将识别不到材料任何内容。
  如不需要识别这项内容，可前往「设置 → 识别」里将其关闭。
  **白名单功能已关闭。** → 提示完**自动把这个白名单关掉**
- **黑名单空**：只提醒「不会过滤任何材料」，**不自动关**
  （黑名单空不影响识别）

⚠ 主窗口 `show_page()` 里必须在 `setCurrentIndex` **之前**调上一页的
`on_hide()`，否则用户已经走了才跳提示。

### 这一轮踩的坑（都值得记）

1. ❌ **`Accordion` 的 `body_widget` 会被 `_extract_body_items` 逐项
   重新包卡片** —— 把「开关」和「配置卡片」丢进去，开关就不在卡片头上了、
   配置卡片也被埋进空壳卡片里显不出来。要放自己的卡片就**别用 body_widget**，
   改用 `qt_widgets.SwitchAccordion`（头部自带开关、内容区原样放控件）。
2. ❌ **用 `isVisible()` 判断"显没显示"在离屏测试里永远不可靠** ——
   它要求**所有祖先都可见**（含顶层窗口）；测试里顶层窗口没 show，
   整棵树 `isVisible()` 都是 False。要判断控件自己有没有被隐藏，用
   **`isHidden()`**。
3. ❌ 图标名必须从 `qt_icon.ICONS` 里挑 —— 写了个不存在的名字
   （`ban` / `shield-check` / `tag`）不会报错，但会被当成 emoji 文字画出来，
   字戳出图标框。现成的 61 个名字见 `qt_icon.ICONS`。
4. ❌ 离屏截图只 `processEvents()` 不够，卡片会叠在一起看着像布局 bug ——
   要显式把各级 `layout().activate()` 跑一遍，几何才是对的。



### 「数值与文字」只剩两项

用户要求只留 **显示数值 / 显示标题**，其余**直接删掉**（不用挪地方）：
前缀、后缀、显示这一项、千分位、大数用万、数值为 0 时隐藏、每小时速率、
标题放数值下面。

⚠ **配置模型里的字段全部保留、并且照旧生效**（直播间预设的 `prefix="×"`
就是这么来的），只是不再给界面入口。别顺手从 `bar_items._item()` 里删掉。
项目的显示 / 隐藏改由标签栏上的 Switch 控制。



### ①② 是同一个根因（Qt 的老坑，值得记住）

`BarEditorDialog` 的 QSS 里有一条 `QLabel { ... }`。Qt 的样式引擎会因为
QLabel 从级联里拿到了 `background`，**把它的 `frameWidth()` 当成 1 顺手画出来**。
实测证据（像素级）：

| 现象 | 位置 | 修前 | 修后 |
|---|---|---|---|
| ② 标签文字外面的圆角细框 | label 矩形 `(243,54) 36x24` | 108 个强调色像素 | 0 |
| ① 侧栏选中项文字后面的深色药丸 | label 矩形 `(72,100) 28x40` | 881 个深色(<30)像素 | 0 |

判据不是「眼睛看」，是**数 label 矩形里的异常像素**：
框 = 一个包围盒铺满整个 label 的连通块（文字是多个分散小块，容易误判）。

修法：给 label 显式补 `background: transparent; border: none;`。
- 本文件自己的标签走 `_lab()`，已经自带；
- **`NavButton` 的 label 我们管不着**（主界面控件），拿到后补一刀 `_fix_label_bg()`。
  ⚠ `NavButton.set_active()` 每次都重设样式，所以 **`_nav()` 里必须再补一次**。

## 还差 1 条（用户 8 条意见里剩的）

| # | 问题 | 在哪改 |
|---|---|---|
| — | 悬浮窗本身显示 StatGI 的 **logo 和名字**（用户明确要过，对应主界面左上角那块） | 见下面「还没做的功能」 |

## 验证方式（重要）

**不要只看 `py_compile`。** 它只查语法，不查运行时。已经栽过好几次：

```
bar_items.current()      语法对，一跑 AttributeError → 编辑器打不开
self.preset_dd           语法对，一跑 AttributeError → 预设存不了
self._w_op / _f_title    语法对，一跑 AttributeError → 项目加载崩
```

**必须真的构造一次窗口 + 点一遍**：

```python
dlg = qt_bar_editor.BarEditorDialog(w, 150); dlg.show()
for _ in range(8): app.processEvents()
dlg._nav(1); dlg._pick_tab(0)          # 切页、点标签
dlg._set_visible(0, False)             # 开关
dlg._apply_to_all()                    # 批量
```

✅ **现成的自动化版本**（离屏跑，分步打印，每步都 PASS/FAIL）：

```
python -u _morph\verify_fast.py            # 编辑器基础 + ① ② ⑤
python -u _morph\verify_icons.py           # 图标库 / 导入 / 预设提示
python -u _morph\verify_update_icons.py    # 自动更新不冲掉用户图标
python -u _morph\verify_round4.py          # 下方图标 / 底部按钮 / 编辑提示
python -u _morph\verify_round5.py          # 数值与文字只剩两项 / 预设只读 / 复制
```

⚠⚠ **`data/` 在 .gitignore 里，被覆盖了没得回滚。**
这些脚本都通过 `_morph/verify_common.py` 把数据目录改到
`_morph/_tmp_verify_data`，**不碰真实 `data/`**，并且收尾时
`verify_common.finish()` 会：
1. 比对真实配置的内容哈希；
2. **发现被动过就自动还原**（不只是报个警）。

已实测：guard 之后把 `data/bar_items.json` 从 4 项改坏成 2 项，
`finish()` 能检测到并还原回 4 项。

新写验证脚本务必照样做。另：**卡死的旧 python 进程**是真凶之一，
跑验证前后各 `Get-Process python` 看一眼。

⚠ 离屏测试时**弹窗会卡死**，要先打桩：
```python
QMessageBox.question = staticmethod(lambda *a, **k: QMessageBox.Yes)
QInputDialog.getText = staticmethod(lambda *a, **k: ("名字", True))
```

⚠⚠ **别在离屏测试里碰嵌套事件循环**（`QMenu.exec` / `QDialog.exec`），
会永久卡住。要测就走 `QTimer.singleShot(0, dlg.accept)` 模拟用户点击。
（这次为此白等了两轮 120 秒超时。）

## 数据层（不用动，已跑通）

- `bar_items.py` —— 配置模型：动态项目列表 + 整体窗口 + 预设 CRUD
  存 `data/bar_items.json`、`data/bar_presets.json`
- `statgi_qt/qt_bar.py` —— 悬浮窗渲染（`StatGiBar` / `ItemCard` / `reload()`）

## 还没做的功能

- **悬浮窗本身显示 StatGI 的 logo 和名字**（用户明确要过，对应主界面左上角那块）

## 踩过的坑清单

1. ❌ 手搓 `QTreeWidget` 当侧边栏 —— 那是文件浏览器控件
2. ❌ 删控件不删引用 —— 运行时才炸，`py_compile` 查不出来
3. ❌ 同一个方法定义两遍 —— Python 用最后一个，改了等于没改（`_tab_presets` 栽过）
4. ❌ 同一个文件用脚本反复打补丁 —— 补到后面看不见全貌
5. ❌ 只看 `py_compile` 就提交 —— 编辑器打不开那次
6. ❌ 靠猜用户要什么 —— "像设置里面那样"猜了四轮才问对
7. ❌ 用了没 import 的名字 —— `QMenu` 用了却没 import，右键菜单一按就 `NameError`
8. ❌ 靠眼睛看截图判断样式 —— 把 QFrame 的圆角边误判成 label 的框，绕了好几轮。
   **要数像素**（label 矩形内的异常像素数），别猜
9. ❌ 在离屏测试里调嵌套事件循环 —— `QMenu.exec` / `exec()` 会永久卡死
10. ❌ 验证脚本直接写真实 `data/` —— 把用户配置覆盖成预设了。
    **一律先 `verify_common.redirect_data_dir()`**
11. ❌ 把 `%TEMP%` 当可写目录 —— 沙箱下 `QImage.save` 会静默返回 False，
    测试素材放仓库里
12. ❌❌ **后台任务超时被 kill，进程其实还活着** —— 最坑的一次：
    8 个 2:25~2:31 启动的 python 一直没死，等我 02:53 恢复好用户配置后，
    它们在后台又按旧状态把 `data/bar_items.json` 写了一遍，
    看起来就像「刚修好又坏了」。同一天被这么坑了三次。
    **症状**：配置反复被改回预设、验证脚本明明隔离了却还是被动。
    **排查**：`Get-Process python`，把十分钟前启动的全 kill 掉。
    **防住**：`verify_common.finish()` 收尾比对 + **自动还原**真实配置。
13. ❌ 测「自动还原」时在脚本**启动前**改坏配置 —— 那样 guard 记录的快照
    里就已经是坏值，永远测不出还原。要在一个进程里：先 guard、再改坏、
    最后 finish，才能真测到。

