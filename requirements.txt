# StatGI 运行依赖
# 说明：下面写的是开发时实测通过的版本（>= 表示不低于该版本）
#
# 安装：pip install -r requirements.txt

# ---------- 界面 ----------
# 界面用 Qt（PySide6）。v0.7 及以前用的是 Tkinter + CustomTkinter，
# 那套源码在 v0.9 里已经删掉了，所以不再需要那两个依赖。
PySide6>=6.6

# ---------- 屏幕捕捉 ----------
mss>=10.0

# ---------- 图像处理 ----------
pillow>=10.0
numpy>=1.24
opencv-python-headless>=4.8

# ---------- 文字识别（离线中文 OCR，不需要 GPU）----------
rapidocr-onnxruntime>=1.2.3
onnxruntime>=1.16          # rapidocr / AI 主界面判断用

# ---------- 直播接口 ----------
flask>=3.0

# ---------- 打包成 exe（只有自己打包时才需要）----------
pyinstaller>=6.0
