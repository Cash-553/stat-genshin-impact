# StatGI 运行依赖
# 说明：下面写的是开发时实测通过的版本（>= 表示不低于该版本）
#
# 安装：pip install -r requirements.txt

# ---------- 界面（V0.8 起用 Qt）----------
PySide6>=6.6               # 界面框架。V0.7 及以前用的是最下面那两个

# ---------- 屏幕捕捉 ----------
mss>=10.0

# ---------- 图像处理 ----------
pillow>=10.0
numpy>=1.24
opencv-python-headless>=4.8

# ---------- 文字识别（离线中文 OCR，不需要 GPU）----------
rapidocr-onnxruntime>=1.2.3
onnxruntime>=1.16          # rapidocr / AI 主界面判断用

# ---------- 直播接口 ----------
flask>=3.0

# ---------- 打包成 exe（只有自己打包时才需要）----------
pyinstaller>=6.0

# ==========================================================
# 旧版 Tk 界面（v0.7 及以前）
# 仓库里还留着那套源码作参考，但 V0.8 的界面已经不用它们了 ——
# 只想跑 V0.8 的话可以不装这三行。
# ==========================================================
# customtkinter>=6.0       # 旧版半透明玻璃背景用到了 6.0 的内部结构
# pystray>=0.19            # 旧版托盘（V0.8 改用 Qt 自带的托盘）
# packaging>=23.0          # customtkinter 的依赖，打包时容易被漏掉
